# Next.js local development env file
# Copy to .env.local and fill with real values

# JWT signing secret for server-side tokens
JWT_SECRET="change-me"

# HMAC secret used to hash email+otp+expiry
HMAC_SECRET="change-me"

# Google OAuth credentials for Gmail API access
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Notes:
# - .env.local is only used by `next dev`/`next build` (Node runtime)
# - For Cloudflare Workers preview/deploy, use `.dev.vars` locally and `wrangler secret put` in production
